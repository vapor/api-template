{
    "port": "$PORT:8080",
    "_comment":
    "The $PORT:8080 call tells the json file to see if there is any value at the 'PORT' environment variable. If there is no value there, it will fallback to '8080'",
    "host": "0.0.0.0",
    "securityLayer": "none",
    "_comment":
    "It's very rare that a server manages its own TLS. More commonly, vapor is served behind a proxy like nginx."
}
