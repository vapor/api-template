on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: test toolbox
    steps:
    - name: print new help
      id: hello
      uses: vapor/toolbox@action
      with:
        command: new --help
