on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    name: test toolbox
    steps:
    - name: foo
      uses: vapor/action/deploy@master
      with:
        env: foo
    - name: bar
      uses: vapor/action/deploy@master
      with:
        env: bar
