import Fluent
import Vapor

// Called before commands run after boot.
public func boot(_ app: Application) throws {
//    do {
//        try print(MigrationLog.query(on: app.db).all().wait())
//    } catch {
//        print(error)
//    }
}
